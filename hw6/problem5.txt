一种设计：
前16为虚页号，后16位为页内偏移
对于虚页号进行一次hash算法，利用hash后的值来匹配页表项
页表的组织形式为，表的
Index为物理地址的前16位，OFFSET为后16位。
在表项中前16位存储对应的虚页号，后16位存储对应的进程信息，例如权限和进程号等等。
在表中利用hash（虚页号+进程号）找到对应的物理地址后进行权限比对
然后在把对应的物理地址+OFFSET也就是虚地址的后16位作为真实地址

HASH算法的一种方式是普通HASH后，如果冲突，那么往后挪一位，可以参见数据结构的证明，期望仍是O(1)
